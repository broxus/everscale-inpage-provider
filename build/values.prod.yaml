service:
  port: 80
ingress:
  enabled: true
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
    kubernetes.io/tls-acme: 'true'
  # hosts:
  #   - host: everscale-inpage-provider.broxus.gitlab.io
  #     paths:
  #       - path: /
  #         pathType: ImplementationSpecific
  # tls:
  #   - secretName: everscale-inpage-provider.broxus.gitlab.io-tls
  #     hosts:
  #       - everscale-inpage-provider.broxus.gitlab.io

livenessProbe:
  tcpSocket:
    port: 80
  initialDelaySeconds: 5
  periodSeconds: 10

readinessProbe:
  tcpSocket:
    port: 80
  initialDelaySeconds: 5
  periodSeconds: 10
